# User Profiles Lab
## Task 1: Custom Environment Variable
* **Objective:** Set a custom environment variable (`MY_CUSTOM_VARIABLE`) to test how the login profile file (`.profile`) loads.
* **Proof:** The modified `.profile` file is included in this repository, showing the line `export MY_CUSTOM_VARIABLE="My Profile Is Loaded"`
## Task 2: Interactive Shell Customization
* **Objective:** Add a welcome message to `~/.bashrc` to test its execution on non-login interactive shells.
* **Proof:** The modified `.bashrc` file is included in this repository, showing the `echo` command that displays the welcome message.
